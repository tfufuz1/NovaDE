// This file declares the `providers` submodule for `global_settings`.
// It can be populated with specific provider implementations later.
// For now, it exists to resolve the module not found error.

// Example of what might go here later:
// pub mod filesystem_provider;
// pub mod memory_provider;
